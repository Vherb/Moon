node_modules/
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Production build (regenerable)
build/

# Large binary assets (>50MB) — too big for GitHub without LFS
# These FBX/binary files must be shared separately or via Git LFS
*.fbx
*.FBX
*.map
node_modules
